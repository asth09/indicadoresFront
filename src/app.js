import { library } from '@fortawesome/fontawesome-svg-core';
import { fas } from '@fortawesome/free-solid-svg-icons'; // Importa TODOS los iconos solid
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

library.add(fas); // Añade todo el pack a la librería global